<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.advisor.agriot.zeon.agriadvisor">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".hspotConnect"
            android:label="@string/add_device">
            android:parentActivityName = "com.advisor.agriot.zeon.agriadvisor.MainActivity" >
            <!-- The meta-data element is needed for versions lower than 4.1 -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.advisor.agriot.zeon.agriadvisor.MainActivity" />
        </activity>
        <activity
            android:name=".confiqureDevice"
            android:label="@string/confiqure_device" />
        <activity
            android:name=".sensorData"
            android:label="FIELD DATA" />
        <activity android:name=".SignupActvity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".mainWindow" />
        <activity android:name=".LoginActivity" />
        <activity
            android:name=".UserProfile"
            android:label="USER PROFILE" />
        <activity
            android:name=".MainActivity"
            android:label="@string/home"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".TestActivity" />
        <activity android:name=".EditUserProfileActivity" />
        <activity android:name=".MydevicesActivitty" />
        <activity
            android:name=".cropSuitability"
            android:label="@string/crop_suitability" />
        <activity
            android:name=".cropSelect"
            android:label="@string/crop_details" />
        <activity android:name=".exactConnect" />
        <activity
            android:name=".store"
            android:label="@string/agri_stock" />
        <activity android:name=".currentCrops"></activity>
    </application>

</manifest>